= render "products/sub_header"
.purchase_detail_wrapper
  .purchase_detail_wrapper__form
    .top_text
      購入内容の確認
    .hr
    .purchase-product
      .purchase-product__wrapper
        .purchase-product__wrapper__left
          .purchase-product__wrapper__left__image
            = image_tag @product .product_images[0].url.url, class: 'product2'
        .purchase-product__wrapper__right    
          .purchase-product__wrapper__right__name
            %p 
              = @product.name
          .purchase-product__wrapper__right__price
            %p (税込)送料込み
            %p 
              %span ¥
              %p.purchase-product__wrapper__right__price__value
                = @product.price
          
          
    .hr
    .total_price
      .total_price__text
        支払い金額
      .total_price__view
        %span ¥
        %p 
          = @product.price
    .hr
      
    .payment_methods
      .payment_methods__wrapper
        .text
          %p 支払い方法
        .change
          = link_to credit_card_path(params[:id]) do
            変更する
            = icon("fas","chevron-right")
      .creditIndex__container__list__content#purchase-credit
        .creditIndex__container__list__content__left
          .creditIndex__container__list__content__left__logo
            = image_tag "cards/#{@card_src}"
          .creditIndex__container__list__content__left__info
            .creditIndex__container__list__content__left__info__number
              = "**** **** **** " +  @customer_card.last4
            .creditIndex__container__list__content__left__info__limit
              %p 有効期限 
              %span
                = @exp_month + " / " + @exp_year

    .hr
    .shipping
      .wrapper
        .text
          %p 配送先
        .change
          = link_to "#" do
            変更する
            = icon("fas","chevron-right")
      .sending__address
        .post_code
          %p 
            〒
            %span 
              = @adress.post_code
        .address
          %p 
            %span
              -# = @product.prefecture_code.name
              = @adress.prefecture_code.name
            %span
              = @adress.city
            %span
              = @adress.house_number
          %p
            = @adress.building_name
  
    .hr
    .about
      .text
        郵便局/コンビニ受取をご希望の方は、購入後に取引画面から受取場所を変更をすることが可能です。出品者が発送作業を開始した後は受取場所の変更ができませんので、早めに変更を行ってください。
    = link_to pay_product_purchase_path(product_id: @product.id),method: :post do
      .purchase_btn
        .text
          購入する

= render "products/sub_footer"


    
